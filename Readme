1)First we write a Python script to record a video of 20 seconds naming it as 'start_recording.py'
2) Then create a systemd Service for starting the code once the Raspi boots up.
3) Enable and start the systemd service using.
